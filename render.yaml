services:
  - type: web
    name: clipninja
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600 --workers 1 --bind 0.0.0.0:$PORT
    disk:
      name: workspace
      mountPath: /opt/render/project/src/workspace
      sizeGB: 10
